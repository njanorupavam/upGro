{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd server && npm install && npm run build",
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 && ./flutter/bin/flutter config --enable-web && ./flutter/bin/flutter pub get && ./flutter/bin/flutter build web --release --base-href=/ --dart-define=GOOGLE_CLIENT_ID=$GOOGLE_CLIENT_ID",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/health",
      "destination": "/api/health"
    },
    {
      "source": "/auth/:path*",
      "destination": "/api/auth/:path*"
    },
    {
      "source": "/tasks/:path*",
      "destination": "/api/tasks/:path*"
    },
    {
      "source": "/habits/:path*",
      "destination": "/api/habits/:path*"
    },
    {
      "source": "/goals/:path*",
      "destination": "/api/goals/:path*"
    },
    {
      "source": "/analytics/:path*",
      "destination": "/api/analytics/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

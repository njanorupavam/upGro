{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "echo Skipping npm install",
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 && ./flutter/bin/flutter config --enable-web && ./flutter/bin/flutter pub get && ./flutter/bin/flutter build web --release --base-href=/ --dart-define=API_BASE_URL=${API_BASE_URL:-http://127.0.0.1:4000}",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
